{
  "private": true,
  "name": "cypress-terminal-report-skip-test",
  "version": "1.0.0",
  "description": "This repo contains an example React App, with the tests written in Cypress",
  "scripts": {
    "cy": "cypress open",
    "cy:run": "cypress run"
  },
  "devDependencies": {
    "cypress": "7.7.0",
    "cypress-terminal-report": "3.2.2"
  }
}
